name: social
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^6.0.2
  another_flushbar: ^1.12.32
  google_sign_in: ^6.2.0
  path_provider: ^2.1.5
  pin_code_fields: ^8.0.1     # ✅ moved here
  otp_autofill: ^4.1.0        # ✅ moved here
  isar: ^3.1.0+1              # ✅ moved here
  isar_flutter_libs: ^3.1.0+1 # ✅ moved here

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.6